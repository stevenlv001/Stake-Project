一、索引器是什么？为什么需要它？
通俗理解
想象一下：
1. 区块链就像一个公开的账本，所有人都能看到，但查询很慢
2. 索引器就像一个"记账员"，它的工作是：
    盯着区块链上发生的每一笔交易
    抄写重要的信息到自己的笔记本（数据库）
    整理好方便快速查找
为什么需要索引器？
直接查区块链	            通过索引器查数据库
每次都要调用RPC节点（慢）	 本地数据库查询（快）
不支持复杂查询	            支持SQL任意查询
无法聚合统计	            可以轻松统计总数、平均值等
依赖网络稳定性	            本地数据更可靠
举例：
用户问："我总共质押了多少次？"

❌ 直接查区块链：
   - 要扫描从第1块到最新块的所有区块
   - 逐个检查每个交易是不是你的
   - 可能需要几分钟

✅ 通过索引器：
   SELECT COUNT(*) FROM chain_events 
   WHERE user = '0x1234...' AND event_type = 'Staked'
   - 毫秒级返回结果


二、核心数据结构详解
1. 全局变量（Line 21-29）
```go
var (
    // ========== 队列相关 ==========
    
    // 用户事件队列：暂存待入库的用户操作事件
    // 例如：张三质押10ETH、李四解质押5ETH等
    // 类型：切片（动态数组）
    eventQueue    []EventBatchItem
    
    // 管理员事件队列：暂存待入库的管理员操作事件
    // 例如：添加黑名单、暂停合约、调整参数等
    adminQueue    []AdminBatchItem
    
    // 区块同步记录：记录每个已处理区块的哈希值
    // 作用：检测链重组（分叉）
    // 结构：map[区块号]区块信息
    blockMap      map[uint64]model.BlockSync
    
    // ========== 并发控制 ==========
    
    // 互斥锁：保护队列的线程安全
    // 原因：主循环和定时刷新协程会同时访问队列
    queueMutex    sync.Mutex
    
    // ========== 批量控制 ==========
    
    // 上次入库时间：用于判断是否超时
    lastFlushTime time.Time
    
    // 批量阈值：队列中积累多少个事件就触发入库
    // 默认配置：50个
    batchSize     int
    
    // 超时时间：即使未达到阈值，超过这个时间也强制入库
    // 默认配置：10秒
    batchWaitTime time.Duration
)
```
为什么要用队列？
类比快递分拣中心：
- 包裹（事件）陆续到达
- 先放到传送带（队列）上
- 积累到一定数量（batchSize）
- 统一装车发货（批量入库）

好处：
✓ 减少运输次数（数据库连接）
✓ 提高装载率（批量插入效率）
✓ 降低成本（网络开销）

2. 事件批处理项（Line 31-44）
```go
// EventBatchItem：单个事件的"包装盒"
type EventBatchItem struct {
    // 【核心字段】链上事件的原始数据
    // 包含：区块号、交易哈希、事件类型、用户地址、金额等
    ChainEvent        model.ChainEvent
    
    // 【快捷访问】用户钱包地址
    // 例如："0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb"
    UserAddr          string
    
    // 【金额信息】操作的代币数量（单位：wei）
    // 使用大整数类型，避免精度丢失
    // 例如：1 ETH = 1000000000000000000 wei
    Amount            *big.Int
    
    // 【标记字段】是否为质押事件
    // true = 质押（增加余额）
    // false = 解质押（减少余额）
    IsStake           bool
    
    // 【标记字段】是否为领取收益事件
    // true = 领收益（不改变质押余额）
    IsReward          bool
    
    // 【收益金额】仅在领取收益事件时有值
    Reward            *big.Int
    
    // 【目标账户】用于黑名单等管理操作
    Account           string
    
    // 【限额参数】更新质押限额时的最小值
    MinLimit          *big.Int
    
    // 【限额参数】更新质押限额时的最大值
    MaxLimit          *big.Int
    
    // 【标记字段】是否为添加黑名单事件
    IsBlacklistAdd    bool
    
    // 【标记字段】是否为移除黑名单事件
    IsBlacklistRemove bool
    
    // 【标记字段】是否为更新质押限额事件
    IsStakeLimits     bool
}
```
为什么需要这么多字段？不同事件类型需要的数据不同：
事件类型	需要的字段
质押/解质押	UserAddr, Amount, IsStake
领取收益	UserAddr, Reward, IsReward
添加黑名单	Account, IsBlacklistAdd
更新限额	MinLimit, MaxLimit, IsStakeLimits
统一结构的好处：
✓ 可以用一个队列存储所有事件
✓ 批量处理时逻辑统一
✓ 避免为每种事件创建单独的队列


三、启动流程（从0到1）
Step 1: 初始化索引器（Line 56-70）
```go
func StartIndexer() {
    // 【日志】在控制台打印：索引器开始运行
    logger.Logger.Info("索引器启动")

    // ========== 加载配置 ==========
    
    // 从配置文件读取批量入库阈值
    // config.yaml 中配置：indexer.batch_size: 50
    batchSize = config.GlobalConfig.Indexer.BatchSize
    
    // 从配置文件读取超时时间，并转换为 time.Duration 类型
    // config.yaml 中配置：indexer.batch_wait_seconds: 10
    // 转换后：batchWaitTime = 10秒
    batchWaitTime = time.Duration(config.GlobalConfig.Indexer.BatchWaitSeconds) * time.Second
    
    // 记录当前时间，作为"上次入库时间"的初始值
    lastFlushTime = time.Now()
    
    // ========== 初始化数据结构 ==========
    
    // 创建用户事件队列
    // make([]Type, length, capacity)
    // - length=0: 初始长度为0（空队列）
    // - capacity=batchSize: 预分配容量50（避免频繁扩容）
    eventQueue = make([]EventBatchItem, 0, batchSize)
    
    // 创建管理员事件队列
    adminQueue = make([]AdminBatchItem, 0, batchSize)
    
    // 创建区块记录映射表
    // map[key类型]value类型
    blockMap = make(map[uint64]model.BlockSync)

    // ========== 设置起始位置 ==========
    
    // 从配置的区块号开始扫描
    // 首次运行：start_block = 0（从创世区块开始）
    // 重启后：start_block = 上次处理的区块号（断点续传）
    currentBlock := config.GlobalConfig.Chain.StartBlock
    
    // ========== 启动定时器 ==========
    
    // 创建一个定时器，每3秒发送一次信号
    // ticker.C 是一个channel，可以接收时间信号
    ticker := time.NewTicker(3 * time.Second)
    
    // defer 表示函数退出时执行
    // 确保资源被正确释放
    defer ticker.Stop()

    // ========== 启动后台协程 ==========
    
    // go 关键字：在新协程（轻量级线程）中运行
    // batchFlushTicker 会在后台独立运行，不阻塞主循环
    go batchFlushTicker()

    // ========== 进入主循环 ==========
    
    // for range ticker.C 表示：
    // 每当ticker.C收到信号（每3秒），就执行一次循环体
    for range ticker.C {
        // 主循环逻辑...（后面详细讲解）
    }
}
```
关键概念解释：
1. 协程（Goroutine）
```go
// 普通函数调用（同步）
batchFlushTicker()  // 会阻塞，必须等它执行完才能继续

// 协程调用（异步）
go batchFlushTicker()  // 立即返回，在后台独立运行
```
2. 定时器（Ticker）
```go
// 创建定时器
ticker := time.NewTicker(3 * time.Second)

// ticker.C 是一个channel
// 每3秒会自动发送一个时间值
for t := range ticker.C {
    fmt.Println("当前时间:", t)  // 每3秒打印一次
}
```
3. defer
```go
func example() {
    defer fmt.Println("最后执行")  // 注册清理操作
    fmt.Println("先执行")
    // 函数退出前自动执行defer的内容
}
// 输出：
// 先执行
// 最后执行
```
Step 2: 获取最新区块号（Line 72-77）
```go
for range ticker.C {
    // 【RPC调用】向以太坊节点查询当前最新的区块号
    // 底层调用：eth_blockNumber JSON-RPC方法
    // 返回值：latestBlock = 最新区块号（例如：18500005）
    latestBlock, err := contract.Client.BlockNumber(contract.Ctx)
    
    // 【错误处理】如果调用失败
    if err != nil {
        // 可能的原因：
        // - 网络连接中断
        // - RPC节点宕机
        // - 请求超时
        
        // 记录错误日志，包含具体错误信息
        logger.Logger.Error("获取最新区块失败", zap.Error(err))
        
        // continue：跳过本次循环剩余代码
        // 直接进入下一次循环（3秒后重试）
        continue
    }

    // 如果成功，继续执行后续逻辑
    // latestBlock 现在是有效的区块号
}
```
为什么要查询最新区块？
场景：索引器落后了
- 当前处理到：区块 18500000
- 链上最新：区块 18500005
- 需要追赶：5个区块

如果不查询：
- 不知道有多少新区块需要处理
- 可能漏掉事件

查询后：
- 知道需要处理 18500000 ~ 18500005
- 逐个扫描这些区块
Step 3: 逐个区块扫描（Line 79-93）
```go
// 【外层循环】遍历从 currentBlock 到 latestBlock 的所有区块
// 例如：currentBlock=18500000, latestBlock=18500005
// 需要处理：18500000, 18500001, 18500002, 18500003, 18500004, 18500005
for currentBlock <= latestBlock {
    
    // ========== 步骤3.1：检查链重组 ==========
    
    // handleReorg 函数会检查这个区块是否发生了分叉
    // 如果发生分叉，会回滚错误的数据
    if err := handleReorg(currentBlock); err != nil {
        // 记录错误日志
        // zap.Uint64 格式化输出无符号64位整数
        logger.Logger.Error("链重组处理失败", 
            zap.Uint64("block", currentBlock), 
            zap.Error(err))
        
        // break：跳出当前的 for 循环
        // 注意：只跳出内层循环，不影响外层 ticker 循环
        // 下次ticker信号到来时会重试
        break
    }

    // ========== 步骤3.2：解析事件 ==========
    
    // parseEventsToQueue 函数会：
    // 1. 获取区块信息
    // 2. 查询该区块的事件日志
    // 3. 解析事件并加入队列
    if err := parseEventsToQueue(currentBlock); err != nil {
        // 记录错误日志
        logger.Logger.Error("事件解析入队失败", 
            zap.Uint64("block", currentBlock), 
            zap.Error(err))
        
        // 出错则停止扫描，等待下次重试
        break
    }

    // ========== 步骤3.3：移动到下一个区块 ==========
    
    // 自增运算符：currentBlock = currentBlock + 1
    currentBlock++
    
    // 【持久化进度】更新配置中的起始区块号
    // 这样即使服务突然崩溃，重启后也能从正确的区块继续
    config.GlobalConfig.Chain.StartBlock = currentBlock
}
```
循环执行示例：
假设：
- currentBlock = 18500000
- latestBlock = 18500003

执行过程：

第1轮循环：
  currentBlock (18500000) <= latestBlock (18500003) ✓
  → 处理区块 18500000
  → currentBlock++ → 18500001

第2轮循环：
  currentBlock (18500001) <= latestBlock (18500003) ✓
  → 处理区块 18500001
  → currentBlock++ → 18500002

第3轮循环：
  currentBlock (18500002) <= latestBlock (18500003) ✓
  → 处理区块 18500002
  → currentBlock++ → 18500003

第4轮循环：
  currentBlock (18500003) <= latestBlock (18500003) ✓
  → 处理区块 18500003
  → currentBlock++ → 18500004

第5轮判断：
  currentBlock (18500004) <= latestBlock (18500003) ✗
  → 退出循环


四、事件解析流程（核心逻辑）
Step 4: 解析单个区块（Line 110-153）
这是索引器最核心的函数，我会逐行详细解释：
```go
func parseEventsToQueue(blockNumber uint64) error {
    // ========== 步骤4.1：获取区块完整信息 ==========
    
    // 调用RPC节点的 eth_getBlockByNumber 方法
    // 参数：区块号（转换为 big.Int 类型）
    // 返回：包含所有交易、时间戳、哈希等信息的区块对象
    block, err := contract.Client.BlockByNumber(
        contract.Ctx,                      // 上下文（用于取消、超时等）
        new(big.Int).SetUint64(blockNumber) // 区块号转为大整数
    )
    
    // 如果获取失败，直接返回错误
    // 可能的原因：区块不存在、网络错误、节点故障
    if err != nil {
        return err
    }

    // ========== 步骤4.2：记录区块信息 ==========
    
    // 将区块信息存入内存map
    // 目的：后续检测链重组时对比哈希值
    blockMap[blockNumber] = model.BlockSync{
        BlockNumber: blockNumber,              // 区块号：18500000
        BlockHash:   block.Hash().Hex(),       // 区块哈希：0xabc123...
        ParentHash:  block.ParentHash().Hex(), // 父区块哈希：0xdef456...
    }
    
    // 为什么要记录父区块哈希？
    // 链重组检测时，不仅要比对当前区块，还要验证区块间的链接关系

    // ========== 步骤4.3：构造事件过滤查询 ==========
    
    // 获取要监听的合约地址
    // 从配置文件读取：chain.mining_proxy
    contractAddr := common.HexToAddress(config.GlobalConfig.Chain.MiningProxy)
    
    // 创建过滤器查询对象
    // FilterQuery 告诉RPC节点我们要查什么
    query := ethereum.FilterQuery{
        FromBlock: new(big.Int).SetUint64(blockNumber), // 起始区块
        ToBlock:   new(big.Int).SetUint64(blockNumber), // 结束区块
        // FromBlock == ToBlock 表示只查一个区块
        
        Addresses: []common.Address{contractAddr},      
        // 只查询这个合约地址发出的事件
        // 可以传入多个地址，同时监听多个合约
    }
    
    // ========== 步骤4.4：查询事件日志 ==========
    
    // 调用RPC节点的 eth_getLogs 方法
    // 返回：该区块中指定合约发出的所有事件日志数组
    logs, err := contract.Client.FilterLogs(contract.Ctx, query)
    
    // 如果查询失败，返回错误
    if err != nil {
        return err
    }
    
    // logs 可能为空数组（该区块没有我们关心的事件）
    // 也可能包含多个事件（一个区块可能有多个交易触发事件）

    // ========== 步骤4.5：加锁保护队列 ==========
    
    // 获取互斥锁
    // 防止其他协程（batchFlushTicker）同时修改队列
    queueMutex.Lock()
    
    // defer unlock：函数返回时自动解锁
    // 确保即使发生panic也能释放锁
    defer queueMutex.Unlock()

    // ========== 步骤4.6：遍历并解析所有事件 ==========
    
    // range 遍历数组/切片
    // _ 忽略索引，vLog 是当前元素（事件日志）
    for _, vLog := range logs {
        // 调用解析函数，将原始日志转换为内部结构
        item := parseEventToBatchItem(vLog)
        
        // 如果解析成功（item不为nil）
        if item != nil {
            // 判断是否为管理员事件
            if item.IsAdminEvent() {
                // 转换为管理员事件格式
                // ToAdminBatchItem 会提取相关字段
                adminQueue = append(adminQueue, item.ToAdminBatchItem(vLog))
            } else {
                // 普通用户事件，直接加入队列
                // *item 解引用指针，复制结构体值
                eventQueue = append(eventQueue, *item)
            }
        }
        // 如果 item == nil，说明是不认识的事件类型，跳过
    }

    // ========== 步骤4.7：检查是否需要批量入库 ==========
    
    // 触发条件（满足任一即可）：
    // 1. 用户事件队列达到阈值
    // 2. 管理员事件队列达到阈值
    if len(eventQueue) >= batchSize || len(adminQueue) >= batchSize {
        // 记录日志：显示两个队列的当前长度
        logger.Logger.Info("批量入库触发：数量达到阈值", 
            zap.Int("user_events", len(eventQueue)), 
            zap.Int("admin_events", len(adminQueue)))
        
        // 执行批量入库
        flushBatch()
    }

    // 返回nil表示成功
    return nil
}
```
关键点解释：
1. 为什么需要加锁？
```go
// 场景：两个协程同时操作队列

协程A（主循环）：
  eventQueue.append(event1)
  
协程B（定时刷新）：
  eventQueue.clear()

如果没有锁：
  → 可能导致数据丢失或竞态条件

有锁保护：
  协程A获得锁 → 添加事件 → 释放锁
  协程B获得锁 → 检查队列 → 释放锁
  → 保证数据安全
```
2. append 的作用
```go
// append 向切片末尾添加元素
queue := []int{1, 2, 3}
queue = append(queue, 4)  // queue = [1, 2, 3, 4]
queue = append(queue, 5)  // queue = [1, 2, 3, 4, 5]

// 注意：append 返回新的切片
// 必须赋值回原变量
```
Step 5: 事件类型识别（Line 190-291）
```go
func parseEventToBatchItem(vLog types.Log) *EventBatchItem {
    // 【事件识别原理】
    // 智能合约 emit 事件时，会生成日志（Log）
    // Log.Topics[0] 是事件签名的哈希值
    // 通过这个哈希值可以判断是哪种事件
    
    // switch-case 根据 Topics[0] 的值分支处理
    switch vLog.Topics[0].Hex() {
    
    // ==================== 情况1：质押事件 ====================
    case abi.AbiStakedEventID.Hex():
        // AbiStakedEventID 是预计算的事件签名哈希
        // keccak256("Staked(address,uint256,uint256)")
        
        // 使用ABI库解析事件参数
        // ParseStaked 会自动解码 data 字段
        // 返回结构化的事件对象
        event, _ := contract.MiningContract.ParseStaked(vLog)
        
        // 构造并返回事件项
        return &EventBatchItem{
            // 基础事件信息（存入数据库）
            ChainEvent: model.ChainEvent{
                BlockNumber: vLog.BlockNumber,     // 区块号
                BlockHash:   vLog.BlockHash.Hex(), // 区块哈希
                TxHash:      vLog.TxHash.Hex(),    // 交易哈希
                EventType:   "Staked",             // 事件类型名称
                User:        event.User.Hex(),     // 用户地址
                Amount:      event.Amount.String(), // 质押金额（字符串）
                EventTime:   event.Time.Uint64(),  // 事件时间戳
            },
            // 额外字段（方便后续处理，不存入数据库）
            UserAddr: event.User.Hex(), // 用户地址（冗余）
            Amount:   event.Amount,     // 金额（大整数格式，方便计算）
            IsStake:  true,             // 标记为质押事件
        }

    // ==================== 情况2：解质押事件 ====================
    case abi.AbiUnstakedEventID.Hex():
        // 解析解质押事件参数
        event, _ := contract.MiningContract.ParseUnstaked(vLog)
        
        return &EventBatchItem{
            ChainEvent: model.ChainEvent{
                BlockNumber: vLog.BlockNumber,
                BlockHash:   vLog.BlockHash.Hex(),
                TxHash:      vLog.TxHash.Hex(),
                EventType:   "Unstaked",           // 事件类型
                User:        event.User.Hex(),
                Amount:      event.Amount.String(),
                EventTime:   event.Time.Uint64(),
            },
            UserAddr: event.User.Hex(),
            Amount:   event.Amount,
            IsStake:  false, // 标记为解质押（与质押区分）
        }

    // ==================== 情况3：领取收益事件 ====================
    case abi.AbiRewardClaimedEventID.Hex():
        event, _ := contract.MiningContract.ParseRewardClaimed(vLog)
        
        return &EventBatchItem{
            ChainEvent: model.ChainEvent{
                BlockNumber: vLog.BlockNumber,
                BlockHash:   vLog.BlockHash.Hex(),
                TxHash:      vLog.TxHash.Hex(),
                EventType:   "RewardClaimed",
                User:        event.User.Hex(),
                Amount:      event.Reward.String(), // 注意：这里是收益金额
                EventTime:   event.Time.Uint64(),
            },
            UserAddr: event.User.Hex(),
            IsReward: true,  // 标记为领取收益
            Reward:   event.Reward,
        }

    // ==================== 情况4：添加黑名单事件 ====================
    case abi.AbiBlacklistAddedEventID.Hex():
        event, _ := contract.MiningContract.ParseBlacklistAdded(vLog)
        
        return &EventBatchItem{
            ChainEvent: model.ChainEvent{
                BlockNumber: vLog.BlockNumber,
                BlockHash:   vLog.BlockHash.Hex(),
                TxHash:      vLog.TxHash.Hex(),
                EventType:   "BlacklistAdded",
                User:        event.Account.Hex(),
                EventTime:   vLog.BlockNumber, // 管理员事件用区块号代替时间
            },
            Account:        event.Account.Hex(),
            IsBlacklistAdd: true, // 标记为添加黑名单
        }

    // ==================== 情况5：移除黑名单事件 ====================
    case abi.AbiBlacklistRemovedEventID.Hex():
        event, _ := contract.MiningContract.ParseBlacklistRemoved(vLog)
        
        return &EventBatchItem{
            ChainEvent: model.ChainEvent{
                BlockNumber: vLog.BlockNumber,
                BlockHash:   vLog.BlockHash.Hex(),
                TxHash:      vLog.TxHash.Hex(),
                EventType:   "BlacklistRemoved",
                User:        event.Account.Hex(),
                EventTime:   vLog.BlockNumber,
            },
            Account:           event.Account.Hex(),
            IsBlacklistRemove: true, // 标记为移除黑名单
        }

    // ==================== 情况6：更新质押限额事件 ====================
    case abi.AbiStakeLimitsUpdatedEventID.Hex():
        event, _ := contract.MiningContract.ParseStakeLimitsUpdated(vLog)
        
        return &EventBatchItem{
            ChainEvent: model.ChainEvent{
                BlockNumber: vLog.BlockNumber,
                BlockHash:   vLog.BlockHash.Hex(),
                TxHash:      vLog.TxHash.Hex(),
                EventType:   "StakeLimitsUpdated",
                Amount:      event.Min.String(),  // 最小限额
                ExtraData:   event.Max.String(),  // 最大限额（存扩展字段）
                EventTime:   vLog.BlockNumber,
            },
            MinLimit:      event.Min,
            MaxLimit:      event.Max,
            IsStakeLimits: true, // 标记为更新限额
        }
    }
    
    // 如果 Topics[0] 不匹配任何已知事件
    // 返回 nil 表示跳过
    return nil
}
```
事件签名哈希是怎么来的？
// 智能合约中的事件定义
event Staked(address indexed user, uint256 amount, uint256 time);

// 计算事件签名：
1. 写出事件原型：Staked(address,uint256,uint256)
2. 计算Keccak256哈希：keccak256("Staked(address,uint256,uint256)")
3. 得到哈希值：0x9a9c5c5e...

// Go代码中预先计算
var AbiStakedEventID = common.HexToHash("0x9a9c5c5e...")

// 当事件发生时，Topics[0] 就是这个哈希值
// 通过对比就能知道是哪种事件


五、批量入库流程
```go
func batchFlushTicker() {
    // 【定时器】每1秒检查一次是否需要入库
    checkTicker := time.NewTicker(1 * time.Second)
    defer checkTicker.Stop()

    // 【无限循环】持续检查
    for range checkTicker.C {
        // 【加锁】防止与主循环冲突
        queueMutex.Lock()
        
        // 【超时判断】两个条件都必须满足：
        // 1. 距离上次入库超过设定时间（默认10秒）
        // 2. 队列中有待处理事件（至少有一个队列非空）
        if time.Since(lastFlushTime) > batchWaitTime && 
           (len(eventQueue) > 0 || len(adminQueue) > 0) {
            
            // 记录日志：说明是超时触发的入库
            logger.Logger.Info("批量入库触发：超时自动提交")
            
            // 执行入库操作
            flushBatch()
        }
        
        // 【解锁】
        queueMutex.Unlock()
    }
}
```
为什么要超时强制入库？
场景1：低频事件
时间线：
00:00 - 用户A质押（队列长度=1）
00:05 - 用户B质押（队列长度=2）
00:10 - 超时触发，强制入库 ← 保证最多延迟10秒

如果没有超时机制：
- 要等到50个事件才入库
- 可能几小时都没有50个事件
- 用户查询不到最新数据

场景2：高频事件
时间线：
00:00.000 - 10个事件（队列长度=10）
00:00.001 - 20个事件（队列长度=30）
00:00.002 - 25个事件（队列长度=55）→ 达到阈值，立即入库

不会等到超时，快速响应
Step 7: 批量写入数据库（Line 293-353）
```go
func flushBatch() {
    // ========== 步骤7.1：空队列检查 ==========
    
    // 如果两个队列都为空，说明没有数据需要入库
    // 直接返回，避免无意义的数据库操作
    if len(eventQueue) == 0 && len(adminQueue) == 0 {
        return
    }

    // ========== 步骤7.2：准备数据容器 ==========
    
    // 声明三个切片变量，用于存储待入库的数据
    // 此时它们都是 nil（未初始化）
    var events []model.ChainEvent         // 用户事件列表
    var adminOps []model.AdminOperation   // 管理员事件列表
    var blockSyncs []model.BlockSync      // 区块同步记录列表

    // ========== 步骤7.3：开启数据库事务 ==========
    
    // Transaction 方法开启一个数据库事务
    // 传入一个函数，该函数接收事务对象 tx
    // 事务保证原子性：要么全部成功，要么全部回滚
    err := db.DB.Transaction(func(tx *gorm.DB) error {
        
        // 【初始化切片】预分配容量，提高性能
        // make([]Type, 0, capacity) 创建空切片但预留空间
        events = make([]model.ChainEvent, 0, len(eventQueue))
        adminOps = make([]model.AdminOperation, 0, len(adminQueue))
        blockSyncs = make([]model.BlockSync, 0, len(blockMap))

        // ========== 步骤7.4：提取用户事件 ==========
        
        // 遍历用户事件队列
        for _, item := range eventQueue {
            // 提取每个事件的 ChainEvent 字段
            // 添加到 events 切片中
            events = append(events, item.ChainEvent)
        }
        
        // 示例：
        // eventQueue 有3个事件
        // → events = [event1.ChainEvent, event2.ChainEvent, event3.ChainEvent]

        // ========== 步骤7.5：提取管理员事件 ==========
        
        // 遍历管理员事件队列
        for _, item := range adminQueue {
            // 提取每个事件的 AdminOp 字段
            adminOps = append(adminOps, item.AdminOp)
        }

        // ========== 步骤7.6：提取区块记录 ==========
        
        // 遍历区块映射表
        // range map 返回 key 和 value
        // _ 忽略 key（区块号），b 是 value（区块信息）
        for _, b := range blockMap {
            blockSyncs = append(blockSyncs, b)
        }

        // ========== 步骤7.7：批量插入用户事件 ==========
        
        // 如果 events 不为空
        if len(events) > 0 {
            // CreateInBatches 分批插入数据
            // 参数1：要插入的数据切片
            // 参数2：每批插入的数量（50条）
            
            // 例如：events 有120条数据
            // 第1批：插入50条
            // 第2批：插入50条
            // 第3批：插入20条
            
            // .Error 获取可能的错误
            if err := tx.CreateInBatches(events, 50).Error; err != nil {
                // 如果插入失败，返回错误
                // 这会导致整个事务回滚
                return err
            }
        }

        // ========== 步骤7.8：批量插入管理员事件 ==========
        
        if len(adminOps) > 0 {
            // 同样分批插入，每批50条
            if err := tx.CreateInBatches(adminOps, 50).Error; err != nil {
                return err // 失败则回滚
            }
        }

        // ========== 步骤7.9：批量插入区块记录 ==========
        
        if len(blockSyncs) > 0 {
            if err := tx.CreateInBatches(blockSyncs, 50).Error; err != nil {
                return err // 失败则回滚
            }
        }

        // ========== 步骤7.10：更新用户质押状态 ==========
        
        // 调用辅助函数，根据事件更新 user_stakes 表
        // 传入事务对象 tx 和事件队列
        batchUpdateUserStake(tx, eventQueue)

        // 返回 nil 表示事务成功
        // GORM 会自动提交事务
        return nil
    })

    // ========== 步骤7.11：错误处理 ==========
    
    // 如果事务执行出错
    if err != nil {
        // 记录错误日志
        logger.Logger.Error("入库失败", zap.Error(err))
        
        // 直接返回，不清空队列
        // 这样下次扫描时会重试这些数据
        // 保证数据不丢失
        return
    }

    // ========== 步骤7.12：清空队列 ==========
    
    // 入库成功后，清空内存中的队列
    // 重新创建空切片，释放内存
    eventQueue = make([]EventBatchItem, 0, batchSize)
    adminQueue = make([]AdminBatchItem, 0, batchSize)
    
    // 清空区块映射表
    // clear 是 Go 1.21+ 的新内置函数
    clear(blockMap)
    
    // 更新上次入库时间
    lastFlushTime = time.Now()

    // ========== 步骤7.13：记录成功日志 ==========
    
    logger.Logger.Info("入库成功", 
        zap.Int("user_events", len(events)), 
        zap.Int("admin_events", len(adminOps)))
}
```
事务的重要性详解：
假设要插入100条事件记录：

❌ 没有事务保护：
第1条 INSERT ✓
第2条 INSERT ✓
...
第50条 INSERT ✗ 失败（网络抖动）
结果：前49条已提交，后51条未插入
→ 数据不完整！

✅ 有事务保护：
BEGIN TRANSACTION
第1条 INSERT ✓
第2条 INSERT ✓
...
第50条 INSERT ✗ 失败
ROLLBACK（自动回滚）
结果：所有数据都未提交
→ 下次重试，保证完整性
CreateInBatches 的优势：
传统方式（逐条插入）：
for _, event := range events {
    db.Create(&event)  // 每次都要：连接→发送SQL→等待响应
}
100条数据 = 100次网络往返 = 约1000ms

批量方式（CreateInBatches）：
db.CreateInBatches(events, 50)
// 第1批：INSERT INTO ... VALUES (...), (...), ... (50条)
// 第2批：INSERT INTO ... VALUES (...), (...), ... (50条)
100条数据 = 2次网络往返 = 约20ms

性能提升：50倍！
Step 8: 更新用户质押状态（Line 355-391）
```go
func batchUpdateUserStake(tx *gorm.DB, queue []EventBatchItem) {
    // ========== 步骤8.1：创建用户映射表 ==========
    
    // map[key]value
    // key: 用户地址（string）
    // value: 用户质押记录指针（*model.UserStake）
    userMap := make(map[string]*model.UserStake)
    
    // 为什么要用 map？
    // 避免重复查询数据库
    // 同一个用户可能有多个事件（多次质押/解质押）

    // ========== 步骤8.2：遍历事件队列 ==========
    
    for _, item := range queue {
        
        // 【跳过收益事件】
        // 领取收益不改变质押金额，无需更新 user_stakes 表
        if item.IsReward {
            continue // 跳过本次循环，处理下一个事件
        }
        
        // ========== 步骤8.3：查找或加载用户记录 ==========
        
        // 检查 map 中是否已有这个用户的记录
        // userMap[item.UserAddr] 尝试获取值
        // ok 是一个布尔值，表示键是否存在
        if _, ok := userMap[item.UserAddr]; !ok {
            // 如果 map 中没有（!ok 表示不存在）
            
            // 创建一个新的 UserStake 结构体
            var stake model.UserStake
            
            // 从数据库查询用户记录
            // WHERE user_address = ?
            // First 返回第一条匹配的记录
            tx.Where("user_address = ?", item.UserAddr).First(&stake)
            
            // 将记录存入 map
            // 即使数据库中不存在（stake.ID=0），也存入 map
            // 避免下次重复查询
            userMap[item.UserAddr] = &stake
        }

        // ========== 步骤8.4：获取用户记录 ==========
        
        // 从 map 中获取用户记录（指针）
        stake := userMap[item.UserAddr]
        
        // 获取当前事件的金额
        amount := item.Amount

        // ========== 步骤8.5：处理新用户 ==========
        
        // 如果 ID 为 0，说明数据库中不存在该用户
        if stake.ID == 0 {
            // 只有质押事件才创建新记录
            // 解质押不可能发生在未质押的用户身上
            if item.IsStake {
                // 填充用户信息
                stake.UserAddress = item.UserAddr
                stake.StakeAmount = amount.String()  // 转为字符串存储
                stake.UpdateTime = uint64(time.Now().Unix()) // 当前时间戳
                
                // 插入数据库
                tx.Create(stake)
                
                // 注意：这里没有更新 map
                // 因为 stake 是指针，Create 后会更新 ID
            }
        } else {
            // ========== 步骤8.6：处理老用户 ==========
            
            // 创建一个大整数，用于计算
            current := new(big.Int)
            
            // 将字符串格式的金额转为大整数
            // SetString 返回 (value, success)
            // 我们只关心 value，所以用 _ 忽略 success
            current.SetString(stake.StakeAmount, 10) // 10表示十进制
            
            // 根据事件类型进行加减运算
            if item.IsStake {
                // 质押事件：增加金额
                // current = current + amount
                current.Add(current, amount)
            } else {
                // 解质押事件：减少金额
                // current = current - amount
                current.Sub(current, amount)
            }
            
            // 将计算结果转回字符串
            stake.StakeAmount = current.String()
            
            // 更新时间戳
            stake.UpdateTime = uint64(time.Now().Unix())
            
            // 保存更新到数据库
            tx.Save(stake)
        }
    }
}
```
优化技巧详解：
场景：用户A在同一批次中有3个事件
- 事件1：质押 10 ETH
- 事件2：质押 5 ETH
- 事件3：解质押 3 ETH

❌ 传统方式（每次都查数据库）：
处理事件1：
  SELECT * FROM user_stakes WHERE address = 'A'  ← 第1次查询
  UPDATE user_stakes SET amount = 10             ← 第1次更新
  
处理事件2：
  SELECT * FROM user_stakes WHERE address = 'A'  ← 第2次查询（重复！）
  UPDATE user_stakes SET amount = 15             ← 第2次更新
  
处理事件3：
  SELECT * FROM user_stakes WHERE address = 'A'  ← 第3次查询（重复！）
  UPDATE user_stakes SET amount = 12             ← 第3次更新

总计：3次查询 + 3次更新 = 6次数据库操作

✅ 优化方式（使用 map 缓存）：
处理事件1：
  if A不在map中 {
    SELECT * FROM user_stakes WHERE address = 'A'  ← 第1次查询
    存入map
  }
  在内存中计算：0 + 10 = 10
  
处理事件2：
  if A不在map中 {  ← A已在map中，跳过查询
    ...
  }
  在内存中计算：10 + 5 = 15
  
处理事件3：
  if A不在map中 {  ← A已在map中，跳过查询
    ...
  }
  在内存中计算：15 - 3 = 12

最后统一 Save                                    ← 第1次更新

总计：1次查询 + 1次更新 = 2次数据库操作

性能提升：3倍！（用户越多，提升越明显）
指针的作用：
```go
// 为什么 map 的值是指针？
userMap := make(map[string]*model.UserStake)

// 如果不用指针：
stake := userMap["0x1234"]  // 复制一份结构体
stake.StakeAmount = "100"   // 修改副本
// map 中的原始数据没有变化！❌

// 使用指针：
stake := userMap["0x1234"]  // 获取指针（引用）
stake.StakeAmount = "100"   // 通过指针修改
// map 中的数据也被修改了！✓
```


六、链重组处理详解
```go
func handleReorg(blockNumber uint64) error {
    // ========== 步骤9.1：查询本地记录的区块信息 ==========
    
    // 从数据库查询之前保存的区块记录
    var syncBlock model.BlockSync
    
    // WHERE block_number = ?
    // First 返回第一条匹配的记录
    if err := db.DB.Where("block_number = ?", blockNumber).First(&syncBlock).Error; err != nil {
        // 如果查询失败（记录不存在）
        // 说明这是首次扫描这个区块，无需检查重组
        // 返回 nil 表示正常
        return nil
    }

    // ========== 步骤9.2：获取链上当前
```
Step 9: 检测和处理分叉（Line 393-419）
```go
func handleReorg(blockNumber uint64) error {
    // ========== 步骤9.1：查询本地记录的区块信息 ==========
    
    // 从数据库查询之前保存的区块记录
    var syncBlock model.BlockSync
    
    // WHERE block_number = ?
    // First 返回第一条匹配的记录
    if err := db.DB.Where("block_number = ?", blockNumber).First(&syncBlock).Error; err != nil {
        // 如果查询失败（记录不存在）
        // 说明这是首次扫描这个区块，无需检查重组
        // 返回 nil 表示正常
        return nil
    }

    // ========== 步骤9.2：获取链上当前的区块信息 ==========
    
    // 调用RPC节点，获取区块链上这个区块号的最新信息
    // 注意：如果发生分叉，这里返回的可能是新的区块
    chainBlock, err := contract.Client.BlockByNumber(
        contract.Ctx, 
        new(big.Int).SetUint64(blockNumber)
    )
    
    // 如果获取失败（网络错误、节点故障等）
    if err != nil {
        // 返回错误，上层会处理
        return err
    }

    // ========== 步骤9.3：对比区块哈希 ==========
    
    // 比较数据库中保存的哈希 和 链上当前的哈希
    if syncBlock.BlockHash != chainBlock.Hash().Hex() {
        
        // 【检测到重组】两个哈希值不同！
        // 说明区块链发生了分叉，这个区块被替换了
        
        // 记录警告日志
        logger.Logger.Warn("检测到链重组，回滚数据", 
            zap.Uint64("block", blockNumber))

        // ========== 步骤9.4：清空内存队列 ==========
        
        // 加锁保护
        queueMutex.Lock()
        
        // 丢弃所有未入库的事件
        // 因为这些事件可能来自错误的分叉链
        eventQueue = make([]EventBatchItem, 0, batchSize)
        adminQueue = make([]AdminBatchItem, 0, batchSize)
        
        // 清空区块映射表
        clear(blockMap)
        
        // 解锁
        queueMutex.Unlock()

        // ========== 步骤9.5：删除数据库中的错误数据 ==========
        
        // 删除该区块及之后的所有事件记录
        // WHERE block_number >= ?
        // 例如：blockNumber = 18500005
        // 会删除 18500005, 18500006, 18500007... 的所有数据
        db.DB.Where("block_number >= ?", blockNumber).Delete(&model.ChainEvent{})
        
        // 删除管理员操作记录
        db.DB.Where("block_number >= ?", blockNumber).Delete(&model.AdminOperation{})
        
        // 删除区块同步记录
        db.DB.Where("block_number >= ?", blockNumber).Delete(&model.BlockSync{})
        
        // ========== 步骤9.6：重置扫描位置 ==========
        
        // 将起始区块设置回这个区块
        // 下次扫描时会重新处理这个区块（正确的版本）
        config.GlobalConfig.Chain.StartBlock = blockNumber
    }
    
    // 返回 nil 表示处理完成
    return nil
}
```
什么是链重组（Reorg）？为什么会发生？
正常情况（无分叉）：
时间 T1: 区块 100 → 区块 101 → 区块 102
时间 T2: 区块 100 → 区块 101 → 区块 102 → 区块 103

索引器处理：
T1: 扫描到区块 102，存入数据库
T2: 扫描到区块 103，继续前进
✓ 一切正常

发生分叉的情况：
时间 T1: 矿工A挖出区块 102A
        区块 100 → 区块 101 → 区块 102A

索引器处理：
T1: 扫描到区块 102A，存入数据库
    记录：block_hash = 0xabc123...

时间 T2: 矿工B挖出更长的链
        区块 100 → 区块 101 → 区块 102B → 区块 103B
        （102B 和 102A 是不同的区块，但区块号相同）

以太坊协议规则：
- 选择最长的链作为主链
- 102B+103B 比 102A 长，所以切换到新链

索引器检测：
T3: 再次查询区块 102
    数据库中的哈希：0xabc123... (102A)
    链上的哈希：     0xdef456... (102B) ← 不同了！
    
→ 检测到重组！需要回滚
为什么要删除该区块之后的所有数据？
假设：
- 区块 102A 中有用户质押事件
- 区块 103A 中有用户领收益事件

发生重组后：
- 区块 102A 被废弃，替换为 102B
- 区块 103A 也被废弃，替换为 103B

如果只删除 102A：
- 103A 的领收益事件还在数据库中
- 但这个收益是基于 102A 的质押计算的
- 现在 102A 不存在了，103A 的数据就错了！

所以要删除 102 及之后的所有数据：
DELETE FROM chain_events WHERE block_number >= 102
→ 清除 102A、103A 的所有记录

然后重新扫描：
→ 处理 102B、103B 的正确数据


七、优雅关闭流程
Step 10: 服务关闭时强制入库（Line 421-426）
```go
func FlushOnShutdown() {
    // 【加锁】保证数据安全
    // 防止在刷新过程中主循环修改队列
    queueMutex.Lock()
    
    // defer 确保即使发生panic也能解锁
    defer queueMutex.Unlock()
    
    // 记录日志：说明是服务关闭触发的强制入库
    logger.Logger.Info("服务关闭，强制刷新批量队列")
    
    // 【强制入库】无论队列中有多少数据，立即写入数据库
    // 即使只有1个事件，也要入库
    // 避免数据丢失
    flushBatch()
}
```
调用位置（cmd/main.go）：
```go
func main() {
    // ... 初始化代码 ...
    
    // 【信号监听】创建通道接收系统信号
    quitChan := make(chan os.Signal, 1)
    
    // Notify 注册要监听的信号
    // SIGINT: Ctrl+C 中断信号
    // SIGTERM: 终止信号（kill命令）
    signal.Notify(quitChan, syscall.SIGINT, syscall.SIGTERM)

    // 【后台协程】等待关闭信号
    go func() {
        // <-quitChan 阻塞等待，直到收到信号
        <-quitChan
        
        // 收到信号后，开始优雅关闭
        logger.Logger.Info("开始优雅关闭服务...")

        // 停止交易追踪器
        txtracker.GetTracker().Stop()
        
        // 【关键】强制刷新索引器队列
        indexer.FlushOnShutdown()

        // 记录日志
        logger.Logger.Info("所有批量数据已入库，服务安全关闭")
        
        // 退出程序
        os.Exit(0)
    }()

    // ... 启动HTTP服务 ...
}
```
优雅关闭的重要性：
场景：服务运行中，队列有30个事件未入库

❌ 直接杀死进程（kill -9）：
- 内存中的30个事件丢失
- 用户查询不到最新的质押记录
- 数据不一致

✅ 优雅关闭（Ctrl+C）：
1. 接收到 SIGINT 信号
2. 调用 FlushOnShutdown()
3. 将30个事件写入数据库
4. 安全退出
- 数据完整
- 无丢失


八、完整执行流程总结
【阶段1：服务启动】
main.go 启动
  ↓
config.InitConfig()          ← 加载配置文件
logger.InitLogger()          ← 初始化日志系统
db.DB.AutoMigrate()          ← 数据库表迁移
redis.InitRedis()            ← 连接Redis
contract.InitContract()      ← 连接RPC节点，加载ABI
  ↓
txtracker.GetTracker().Start()  ← 启动交易追踪器（后台协程）
go indexer.StartIndexer()       ← 启动索引器（后台协程）
  ↓
r.Run(":8080")               ← 启动HTTP服务器

【阶段2：索引器初始化】
StartIndexer() 执行
  ↓
读取配置：batchSize=50, batchWaitTime=10s
初始化队列：eventQueue=[], adminQueue=[]
设置起始区块：currentBlock=0（或上次的位置）
启动定时刷新：go batchFlushTicker()
  ↓
进入主循环：for range ticker.C（每3秒）

【阶段3：持续扫描】
每3秒执行一次：
  ↓
1. 查询最新区块号
   latestBlock = eth_blockNumber()
   
2. 逐个区块扫描
   for currentBlock <= latestBlock:
     ├─ handleReorg(currentBlock)        ← 检查分叉
     ├─ parseEventsToQueue(currentBlock) ← 解析事件
     │   ├─ eth_getBlockByNumber         ← 获取区块
     │   ├─ eth_getLogs                  ← 查询事件
     │   ├─ 识别事件类型                  ← switch Topics[0]
     │   ├─ 解析事件参数                  ← ParseXXX()
     │   └─ 加入队列                      ← append()
     │
     └─ currentBlock++
     
3. 更新进度
   config.StartBlock = currentBlock

【阶段4：后台刷新】
每1秒执行一次（batchFlushTicker）：
  ↓
检查条件：
  ├─ 超时？time.Since(lastFlushTime) > 10s
  └─ 有数据？len(queue) > 0
  
满足条件 → flushBatch()

【阶段5：批量入库】
flushBatch() 执行：
  ↓
1. 开启事务 BEGIN
2. 提取数据
   ├─ events = 从 eventQueue 提取
   ├─ adminOps = 从 adminQueue 提取
   └─ blockSyncs = 从 blockMap 提取
3. 批量插入
   ├─ INSERT INTO chain_events (50条/批)
   ├─ INSERT INTO admin_operations (50条/批)
   └─ INSERT INTO block_sync (50条/批)
4. 更新用户状态
   └─ UPDATE user_stakes
5. 提交事务 COMMIT
6. 清空队列
   ├─ eventQueue = []
   ├─ adminQueue = []
   └─ blockMap = {}
7. 更新时间
   └─ lastFlushTime = now()

【阶段6：服务关闭】
用户按下 Ctrl+C
  ↓
signal.Notify 接收到 SIGINT
  ↓
执行关闭流程：
  ├─ txtracker.Stop()           ← 停止交易追踪
  ├─ indexer.FlushOnShutdown()  ← 强制入库
  └─ os.Exit(0)                 ← 退出程序


九、关键设计要点总结
1. 双定时器设计
主定时器（3秒）：
- 职责：扫描新区块
- 频率：较低，减少RPC调用
- 触发：时间驱动

副定时器（1秒）：
- 职责：检查超时入库
- 频率：较高，保证实时性
- 触发：时间 + 数据量

优势：
✓ 解耦扫描和入库逻辑
✓ 独立调优两个频率
✓ 提高响应速度
2. 批量优化策略
数量触发（快速响应高并发）：
- 阈值：50个事件
- 优点：延迟低（毫秒级）
- 适用：高频场景

时间触发（保证低频及时入库）：
- 超时：10秒
- 优点：延迟可控
- 适用：低频场景

结合使用：
- 高频：数量触发为主
- 低频：时间触发兜底
- 平衡性能和实时性
3. 内存管理技巧
预分配容量：
make([]EventBatchItem, 0, 50)
- 避免频繁扩容
- 减少内存分配次数

指针优化：
map[string]*model.UserStake
- 避免结构体复制
- 直接修改原数据

及时清空：
eventQueue = make(..., 0, batchSize)
- 释放旧内存
- 防止内存泄漏
4. 并发安全保护
互斥锁保护：
queueMutex.Lock()
  访问队列
queueMutex.Unlock()

保护的资源：
- eventQueue
- adminQueue
- blockMap

避免的问题：
- 竞态条件
- 数据丢失
-  panic
5. 容错机制
链重组检测：
- 对比区块哈希
- 自动回滚数据
- 重新扫描

错误重试：
- RPC失败 → 下次重试
- 入库失败 → 保留队列

优雅关闭：
- 强制刷新队列
- 保证数据完整


十、配置调优建议
不同场景的配置
场景1：高频交易（DeFi协议活跃期）
indexer:
  batch_size: 100          # 增大批量，提高吞吐
  batch_wait_seconds: 5    # 缩短超时，降低延迟

chain:
  start_block: latest      # 从最新区块开始
场景2：低频交易（新项目初期）
indexer:
  batch_size: 10           # 减小批量，及时入库
  batch_wait_seconds: 3    # 更短超时

chain:
  start_block: 0           # 从头扫描历史数据
场景3：补全历史数据
indexer:
  batch_size: 200          # 大批量，快速追赶
  batch_wait_seconds: 30   # 长超时，减少入库次数

chain:
  start_block: 0           # 从创世块开始


十一、常见问题排查手册
问题1：索引器滞后
现象：
链上最新区块：18500100
索引器处理到：18500050
滞后：50个区块
排查步骤：
# 1. 查看日志
tail -f logs/app.log | grep "索引器"

# 2. 检查RPC连接
curl -X POST https://your-rpc-node \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"eth_blockNumber","id":1}'

# 3. 检查是否有错误
grep "事件解析入队失败" logs/app.log

# 4. 检查队列长度（添加调试日志）
logger.Info("队列长度", zap.Int("queue", len(eventQueue)))
解决方案：
RPC节点慢 → 更换更快的节点
网络延迟 → 检查网络连接
批量太小 → 增大 batch_size

问题2：数据不一致
现象：
链上查询：userStakes(0x1234) = 10 ETH
数据库查询：SELECT stake_amount FROM user_stakes WHERE user_address = '0x1234' = 8 ETH
-- 1. 检查事件记录
SELECT * FROM chain_events 
WHERE user = '0x1234' 
ORDER BY block_number DESC 
LIMIT 10;

-- 2. 手动对账
-- 计算所有质押和解质押的总和
SELECT 
  SUM(CASE WHEN event_type = 'Staked' THEN CAST(amount AS DECIMAL) ELSE 0 END) -
  SUM(CASE WHEN event_type = 'Unstaked' THEN CAST(amount AS DECIMAL) ELSE 0 END) as calculated_amount
FROM chain_events
WHERE user = '0x1234';

-- 3. 检查是否有链重组
SELECT * FROM block_sync 
WHERE block_number > 18500000 
ORDER BY block_number DESC;
解决方案：
# 重新索引（从某个区块开始）
# 修改 config.yaml
chain:
  start_block: 18500000  # 设置到出问题之前的区块

# 重启服务
go run cmd/main.go

问题3：内存占用过高
现象：
服务运行1天后，内存从100MB增长到2GB
# 1. 使用pprof分析
go tool pprof http://localhost:8080/debug/pprof/heap

# 2. 查看goroutine数量
curl http://localhost:8080/debug/pprof/goroutine?debug=1

# 3. 检查队列是否清空
# 添加日志
logger.Info("队列状态", 
  zap.Int("event_queue", len(eventQueue)),
  zap.Int("admin_queue", len(adminQueue)),
  zap.Int("block_map", len(blockMap)))

解决方案：
确认 flushBatch 被正确调用
检查 clear(blockMap) 是否执行
减小 batch_size
定期重启服务（临时方案）



十二、性能监控指标
// 可以添加以下监控指标

```go
// 1. 扫描速度
blocksPerSecond = processedBlocks / timeElapsed

// 2. 入库延迟
latency = time.Now() - event.Timestamp

// 3. 队列长度
queueLength = len(eventQueue)

// 4. 错误率
errorRate = errorCount / totalRequests

// 5. RPC调用次数
rpcCallsPerSecond = rpcCallCount / timeElapsed
```
Prometheus 集成示例
```go
import "github.com/prometheus/client_golang/prometheus"

var (
    blocksProcessed = prometheus.NewCounter(
        prometheus.CounterOpts{
            Name: "indexer_blocks_processed_total",
            Help: "Total number of blocks processed",
        })
    
    eventsIndexed = prometheus.NewCounterVec(
        prometheus.CounterOpts{
            Name: "indexer_events_indexed_total",
            Help: "Total number of events indexed",
        },
        []string{"event_type"})
    
    queueLength = prometheus.NewGauge(
        prometheus.GaugeOpts{
            Name: "indexer_queue_length",
            Help: "Current length of event queue",
        })
)
```


十三、学习要点总结
核心知识点
区块链事件监听
eth_getLogs RPC方法
事件签名哈希识别
ABI参数解码
批量处理优化
队列缓冲
批量插入
事务保护
并发编程
Goroutine协程
Mutex互斥锁
Channel通信
容错机制
链重组检测
错误重试
优雅关闭
性能优化
预分配内存
Map缓存
分批处理