StakeMining 智能合约核心知识点与面试大全


一、合约架构概览
1.1 继承关系图
StakeMining 合约继承链：

StakeMining
    ↓ inherits
Initializable          ← 可初始化（替代构造函数）
    ↓ inherits
OwnableUpgradeable     ← 所有权管理（只有owner能执行某些操作）
    ↓ inherits
UUPSUpgradeable        ← UUPS升级模式（可升级合约）
    ↓ inherits
PausableUpgradeable    ← 可暂停（紧急情况下停止合约）
    ↓ inherits
ReentrancyGuard        ← 防重入攻击保护


二、核心知识点详解
知识点 1：可升级合约（Upgradeable Contract）
概念解释
传统合约的问题：
// 传统合约部署后无法修改
contract OldContract {
    uint256 public value = 100;
    
    // 发现bug？无法修复！
    // 需要新功能？无法添加！
}
可升级合约的解决方案：
┌─────────────┐         ┌──────────────┐
│   Proxy     │ ──────▶ │ Implementation│
│  (代理合约)  │  delegatecall │  (实现合约)  │
│             │         │              │
│ 存储状态     │         │ 业务逻辑      │
└─────────────┘         └──────────────┘
       ↑                        ↑
   数据存在这里            逻辑在这里
   升级时不变              可以替换
工作原理：
用户调用 Proxy 合约
Proxy 通过 delegatecall 转发到 Implementation
代码在 Implementation 中执行
但数据存储 in Proxy
升级时：更换 Implementation 地址，Proxy 和数据不变
面试题 1.1：UUPS 和 Transparent Proxy 有什么区别？
答案对比表：
特性	                UUPS	                        Transparent Proxy
升级逻辑位置	        在 Implementation 中	            在 Proxy 中
Gas 成本	            更低（每次调用少一层跳转）	            较高
安全性	                需要小心实现 _authorizeUpgrade	    更安全（升级逻辑分离）
复杂度	                较简单	                            较复杂（需要 ProxyAdmin）
OpenZeppelin推荐	    ✅ 推荐	                            ⚠️ legay
代码对比：
// UUPS 模式（本项目使用）
contract StakeMining is UUPSUpgradeable {
    // 升级逻辑在实现合约中
    function _authorizeUpgrade(address newImplementation) 
        internal override onlyOwner {}
}

// Transparent Proxy 模式
// 需要额外的 ProxyAdmin 合约管理升级
// 升级逻辑在 Proxy 中，不在实现合约中
实际例子：
场景：发现收益计算有bug，需要修复

UUPS 升级流程：
1. 部署新的 StakeMining V2（修复bug）
2. 调用 proxy.upgradeTo(newImplementation)
3. 完成！用户数据不变，逻辑已更新

Transparent Proxy 升级流程：
1. 部署新的 StakeMining V2
2. 调用 ProxyAdmin.upgrade(proxy, newImplementation)
3. 完成！多了一层 ProxyAdmin 管理

面试题 1.2：为什么可升级合约不能用构造函数？
// ❌ 错误做法
contract StakeMining {
    constructor() {
        stakeToken = IERC20(0x1234...);  // 这会在实现合约中执行
    }
}

// ✅ 正确做法
contract StakeMining is Initializable {
    function initialize(
        address _stakeToken,
        ...
    ) public initializer {
        stakeToken = IERC20(_stakeToken);  // 这在代理合约中执行
    }
}
原因详解：
1. 构造函数只在部署时执行一次
    部署 Implementation 时执行构造函数
    但用户交互的是 Proxy
    Proxy 的状态没有被初始化！
2. initializer 修饰器的作用
modifier initializer() {
    // 检查是否已经初始化
    require(!_initialized, "Already initialized");
    
    // 标记为已初始化
    _initialized = true;
    
    // 执行函数体
    _;
}
3. _disableInitializers() 的作用
constructor() {
    _disableInitializers();  // 防止直接调用 implementation
}

为什么要禁用？
攻击场景：
1. 黑客直接调用 Implementation.initialize()
2. Implementation 被初始化，占用初始化标志
3. Proxy 无法再初始化（因为只能初始化一次）
4. Proxy 变成废合约！

防护措施：
_disableInitializers() 设置内部标志
使得 Implementation 永远无法被初始化
只有 Proxy 能通过 delegatecall 初始化

知识点 2：收益计算算法
 核心公式
 function _updateReward(address _user) internal {
    StakeInfo storage stakeInfo = userStakes[_user];
    if (stakeInfo.amount == 0) return;

    // 核心计算公式
    uint256 timeDiff = block.timestamp - stakeInfo.lastUpdate;
    uint256 reward = stakeInfo.amount.mul(rewardRate).mul(timeDiff).div(1e18);
    stakeInfo.rewardDebt = stakeInfo.rewardDebt.add(reward);
    stakeInfo.lastUpdate = block.timestamp;
}
公式拆解：
收益 = 质押金额 × 收益率 × 时间差 / 精度调整

例如：
- 质押金额：1000 ETH = 1000 × 10^18 wei
- 收益率：100 wei/秒
- 时间差：3600 秒（1小时）

收益 = 1000×10^18 × 100 × 3600 / 10^18
     = 360,000,000 wei
     = 0.36 ETH
面试题 2.1：为什么要用 rewardDebt（收益债务）这个设计？
答案：这是 MasterChef（SushiSwap）的经典设计模式，叫 "Accrue-on-Demand"（按需累积）
// ❌ 错误做法：每个区块都更新所有用户
function updateAllUsers() external {
    for (uint i = 0; i < users.length; i++) {
        users[i].reward += calculateReward(users[i]);  // Gas爆炸！
    }
}
// 1000个用户 = 1000次计算 = Gas费天价传统方式的问题：
rewardDebt 设计的优势：
// ✅ 正确做法：只在用户操作时更新
function stake(uint256 amount) external {
    _updateReward(msg.sender);  // 只更新当前用户
    // ...
}

function claimReward() external {
    _updateReward(msg.sender);  // 只更新当前用户
    // ...
}
// 1000个用户，只有1个操作 = 1次计算 = Gas便宜
通俗理解：
想象银行利息计算：

❌ 传统方式：
每天给100万客户逐个计算利息
→ 银行累死，手续费高昂

✅ rewardDebt 方式：
记录你上次来银行的时间
等你再来时，一次性计算这段时间的利息
→ 高效，按需计算

例子：
用户A：
- 第1天：存入1000元，lastUpdate=第1天，rewardDebt=0
- 第10天：来取钱
  → 计算：1000 × 利率 × 9天 = 90元利息
  → rewardDebt = 90
  → lastUpdate = 第10天
  
用户B：
- 第1天：存入2000元，lastUpdate=第1天，rewardDebt=0
- 第100天：才来取钱
  → 计算：2000 × 利率 × 99天 = 1980元利息
  → rewardDebt = 1980
  → lastUpdate = 第100天

中间99天，银行什么都没做！
面试题 2.2：为什么除以 1e18？
答案：这是 Solidity 的精度处理问题。背景知识：
Solidity 不支持小数（没有 float/double）
所有数字都是整数
ERC20 代币通常有 18 位小数
举例说明：
// 假设：
stakeInfo.amount = 1000 * 10^18  // 1000 ETH（以wei为单位）
rewardRate = 100                  // 每秒100 wei
timeDiff = 3600                   // 3600秒

// 计算过程：
reward = 1000×10^18 × 100 × 3600 / 10^18
       = 360,000,000,000×10^18 / 10^18
       = 360,000,000,000 wei
       = 0.36 ETH

// 如果不除以 1e18：
reward = 1000×10^18 × 100 × 3600
       = 360,000,000,000×10^18 wei
       = 360,000,000,000 ETH  ← 错了！多了10^18倍
为什么要这样设计？
目的：保持高精度计算

如果直接用小数：
rewardRate = 0.0000000001 ETH/秒  ← Solidity不支持！

所以用整数表示：
rewardRate = 100 wei/秒
           = 100 / 10^18 ETH/秒
           = 0.0000000000000001 ETH/秒

计算时：
(大数 × 大数 × 大数) / 10^18 = 正确结果
答案：
问题1：整数除法精度损失
uint256 reward = stakeInfo.amount.mul(rewardRate).mul(timeDiff).div(1e18);
//                    ↑              ↑           ↑          ↑
//                 可能溢出      可能溢出    可能溢出   精度损失
举例：
假设：
amount = 1 wei
rewardRate = 1 wei/秒
timeDiff = 1 秒

reward = 1 × 1 × 1 / 10^18 = 0  ← 精度丢失！

用户质押1 wei，1秒后收益应该是 0.000000000000000001 ETH
但整数除法得到 0
改进方案：
// 方案1：使用更高精度
uint256 reward = stakeInfo.amount.mul(rewardRate).mul(timeDiff).div(1e12);
// 改为除以 1e12，保留更多精度

// 方案2：使用累积奖励率（Accumulator Pattern）
// Uniswap V2/V3 使用的方案
uint256 accRewardPerShare = totalAccumulatedReward / totalStaked;
user.reward = user.shares * accRewardPerShare - user.rewardDebt;
问题2：未考虑复利
当前设计：单利计算
1000 ETH × 100 wei/秒 × 3600秒 = 固定收益

如果想实现复利：
需要定期将收益自动重新质押
或者让用户手动 compound

知识点 3：安全防护机制
3.1 重入攻击防护
代码：
function stake(uint256 _amount) external whenNotPaused notBlacklisted nonReentrant {
    // ...
    stakeToken.safeTransferFrom(msg.sender, address(this), _amount);
    // ...
}
面试题 3.1：什么是重入攻击？如何防护？
答案：
重入攻击原理：
// ❌ 易受攻击的代码
function withdraw(uint256 amount) external {
    // 步骤1：转账
    token.transfer(msg.sender, amount);  // ← 攻击点
    
    // 步骤2：更新余额
    balances[msg.sender] -= amount;      // ← 太晚了！
}

// 攻击合约
contract Attacker {
    function attack() external {
        victim.withdraw(100);  // 调用受害者合约
    }
    
    // ERC20 的回调（如果是ETH则是 fallback）
    function onTransferReceived(...) external {
        // 递归调用！
        victim.withdraw(100);  // 再次提现
        victim.withdraw(100);  // 再次提现
        // ... 直到耗尽合约余额
    }
}
攻击流程：
第1次调用 withdraw(100):
  1. 转账100给攻击者 ✓
  2. 触发攻击者的回调
     → 第2次调用 withdraw(100):
       1. 转账100给攻击者 ✓ (此时余额还没更新！)
       2. 触发回调
          → 第3次调用 withdraw(100):
            ...
  3. 更新余额：balances[attacker] -= 100

结果：攻击者提走了1000 ETH，但余额只减了100
防护方法：
// ✅ 方法1：Checks-Effects-Interactions 模式
function withdraw(uint256 amount) external {
    // 1. Checks：检查条件
    require(balances[msg.sender] >= amount);
    
    // 2. Effects：更新状态（先更新！）
    balances[msg.sender] -= amount;
    
    // 3. Interactions：外部调用（最后调用）
    token.transfer(msg.sender, amount);
}

// ✅ 方法2：重入锁（本项目使用）
modifier nonReentrant() {
    require(_status != _ENTERED, "ReentrancyGuard: reentrant call");
    _status = _ENTERED;
    _;
    _status = _OPEN;
}

function stake(uint256 _amount) external nonReentrant {
    // 即使先转账，也无法重入
    stakeToken.safeTransferFrom(msg.sender, address(this), _amount);
}
本项目的位置：
function stake(uint256 _amount) external whenNotPaused notBlacklisted nonReentrant {
    //                     ↑              ↑              ↑
    //                  暂停检查       黑名单检查      重入保护
    
    _updateReward(msg.sender);
    stakeToken.safeTransferFrom(msg.sender, address(this), _amount);  // 外部调用
    stakeInfo.amount += _amount;  // 状态更新
}
注意： 本项目虽然用了 nonReentrant，但顺序不是最优的。最佳实践应该是先更新状态再转账。
3.2 SafeERC20
代码：
using SafeERC20 for IERC20;

stakeToken.safeTransferFrom(msg.sender, address(this), _amount);
面试题 3.2：为什么要用 SafeERC20？和普通 transfer 有什么区别？
答案：
普通 ERC20 的问题：
// ❌ 不安全的做法
IERC20(token).transfer(to, amount);

// 问题1：有些代币 transfer 返回 false 而不是 revert
// 问题2：有些代币没有返回值（如USDT）
// 问题3：有些代币在余额不足时不报错
真实案例：
2017年，多个项目因未检查 transfer 返回值而损失资金

例子：
contract Vulnerable {
    function deposit() external {
        token.transferFrom(msg.sender, address(this), 100);
        // 如果 transferFrom 返回 false，这里不会revert
        // 但余额没有增加！
        balances[msg.sender] += 100;  // ← 错误记账
    }
}
SafeERC20 的解决：
// ✅ 安全做法
using SafeERC20 for IERC20;

token.safeTransferFrom(from, to, amount);

// SafeERC20 内部逻辑：
function safeTransferFrom(IERC20 token, address from, address to, uint256 value) {
    (bool success, bytes memory data) = address(token).call(
        abi.encodeWithSelector(token.transferFrom.selector, from, to, value)
    );
    
    // 检查返回值
    require(success && (data.length == 0 || abi.decode(data, (bool))), "SafeERC20: transfer failed");
}
支持的异常情况：
代币类型	行为	SafeERC20处理
标准ERC20	返回 true/false	✓ 检查返回值
USDT类	无返回值	✓ 允许空返回
异常代币	revert on error	✓ 捕获revert

知识点 4：访问控制
4.1 Ownable
代码：
function pause() external onlyOwner {
    _pause();
}

function addBlacklist(address _account) external onlyOwner {
    blacklist[_account] = true;
}
面试题 4.1：onlyOwner 是如何实现的？有什么风险？
答案：
实现原理：
// OpenZeppelin Ownable 简化版
contract Ownable {
    address private _owner;
    
    constructor() {
        _owner = msg.sender;  // 部署者成为owner
    }
    
    modifier onlyOwner() {
        require(msg.sender == _owner, "Ownable: caller is not the owner");
        _;
    }
    
    function transferOwnership(address newOwner) external onlyOwner {
        _owner = newOwner;
    }
}
风险点：
风险1：Owner 私钥泄露
场景：
- Owner 是个人钱包
- 私钥被盗
- 攻击者可以：
  ✓ 暂停合约
  ✓ 将所有用户加入黑名单
  ✓ 修改收益率为0
  ✓ 升级合约为恶意版本

防护：
- 使用多签钱包（Gnosis Safe）
- 2/3 或 3/5 签名才能执行
风险2：Owner 失联
场景：
- Owner 私钥丢失
- 合约无法升级
- 无法暂停（发现bug时）
- 无法调整参数

防护：
- 设置备用 owner
- 使用 Timelock（延迟执行）
- 去中心化治理（DAO）
风险3：Owner 作恶
场景：
- Owner 故意降低收益率
- Owner 将自己加入白名单绕过限制
- Owner 升级合约窃取资金

防护：
- 链上治理投票
- 时间锁（修改参数需等待7天）
- 最大权限限制（不能直接转走资金）

知识点 5：事件（Events）
event Staked(address indexed user, uint256 amount, uint256 time);
event RewardClaimed(address indexed user, uint256 reward, uint256 time);

emit Staked(msg.sender, _amount, block.timestamp);
面试题 5.1：为什么要用 indexed 关键字？有什么好处？
答案：
indexed 的作用：
// 最多3个参数可以 indexed
event Staked(
    address indexed user,    // ← 可以被过滤
    uint256 amount,          // ← 不能被过滤
    uint256 time             // ← 不能被过滤
);
好处1：高效查询
// ✅ 可以快速查询特定用户的事件
const events = await contract.queryFilter(
    contract.filters.Staked(userAddress)  // 只查这个用户
);

// ❌ 如果没有 indexed，需要遍历所有事件
const allEvents = await contract.queryFilter(contract.filters.Staked());
const userEvents = allEvents.filter(e => e.args.user === userAddress);
// 慢100倍！
好处2：节省Gas
非 indexed 参数：存储在 data 字段（完整存储）
indexed 参数：存储在 topics 字段（哈希存储，更紧凑）

Gas 对比：
- 非 indexed address: ~2000 gas
- indexed address: ~1000 gas
限制：
// ❌ 错误：超过3个 indexed
event MyEvent(
    address indexed a,
    address indexed b,
    address indexed c,
    address indexed d  // ← 编译错误！
);

// ✅ 正确：最多3个
event MyEvent(
    address indexed a,
    address indexed b,
    address indexed c,
    address d  // 第4个不能 indexed
);


三、Foundry 测试框架知识点
知识点 6：测试setUp
代码：
function setUp() public {
    stakeToken = new TestToken("Stake Token", "STK");
    rewardToken = new TestToken("Reward Token", "RWD");
    
    StakeMining implementation = new StakeMining();
    proxyAdmin = new ProxyAdmin(address(this));
    
    bytes memory initData = abi.encodeWithSelector(
        StakeMining.initialize.selector,
        address(stakeToken),
        address(rewardToken),
        REWARD_RATE,
        STAKE_MIN,
        STAKE_MAX
    );
    
    proxy = new TransparentUpgradeableProxy(
        address(implementation),
        address(proxyAdmin),
        initData
    );
    
    stakeMining = StakeMining(address(proxy));
}
 面试题 6.1：Foundry 和 Hardhat 有什么区别？为什么选择 Foundry？
 答案对比：
特性	    Foundry	                Hardhat
语言	    Solidity（原生）	    JavaScript/TypeScript
速度	    极快（Rust编写）	    较慢（Node.js）
测试写法	Solidity 测试合约	    JS 测试脚本
Fork测试	内置支持	            需要插件
模糊测试	内置 fuzz testing	    需要额外配置
调试体验	优秀（trace清晰）	    一般
学习曲线	陡峭（需懂Solidity）	平缓（JS开发者友好）
生态	    新兴，增长快	        成熟，插件多
常用 cheatcodes：
vm.warp(timestamp)        // 设置区块时间
vm.roll(blockNumber)      // 设置区块号
vm.deal(address, amount)  // 给地址发送ETH
vm.store(slot, value)     // 直接修改存储槽
vm.load(slot)             // 读取存储槽

四、扩展面试题（高频考点）
扩展题 1：Gas 优化
❓ 问：这个合约有哪些 Gas 优化空间？
答案：
优化1：减少 SLOAD（存储读取）
// ❌ 当前代码
function stake(uint256 _amount) external {
    StakeInfo storage stakeInfo = userStakes[msg.sender];  // SLOAD
    _updateReward(msg.sender);                              // 内部又SLOAD
    // ...
}

function _updateReward(address _user) internal {
    StakeInfo storage stakeInfo = userStakes[_user];  // 重复SLOAD
    // ...
}

// ✅ 优化：传递 storage 引用
function stake(uint256 _amount) external {
    StakeInfo storage stakeInfo = userStakes[msg.sender];
    _updateReward(stakeInfo);  // 传递引用，避免重复读取
    // ...
}

function _updateReward(StakeInfo storage stakeInfo) internal {
    // 直接使用，无需再次读取
}
Gas 节省： 每次 SLOAD ~2100 gas，减少2次 = 节省 4200 gas
优化2：使用自定义错误
// ❌ 当前代码
require(_amount > 0, "stake amount must be greater than zero");
// revert 字符串需要编码，Gas 高

// ✅ 优化
error InvalidAmount(uint256 amount);

if (_amount == 0) revert InvalidAmount(_amount);
// 自定义错误只需4字节 selector，Gas 低
Gas 节省： 约 50-100 gas/次

优化3：打包结构体
// ❌ 当前代码
struct StakeInfo {
    uint256 amount;      // 32 bytes
    uint256 rewardDebt;  // 32 bytes
    uint256 lastUpdate;  // 32 bytes
}
// 总共 96 bytes，占用 3 个存储槽

// ✅ 优化：如果数值范围允许
struct StakeInfo {
    uint128 amount;      // 16 bytes
    uint128 rewardDebt;  // 16 bytes  ← 打包到1个槽
    uint64 lastUpdate;   // 8 bytes
    uint64 reserved;     // 8 bytes   ← 预留，凑满32 bytes
}
// 总共 48 bytes，占用 2 个存储槽
Gas 节省： 减少1个存储槽，每次读写节省 ~2100 gas

扩展题 2：安全性
问：如果奖励代币池空了怎么办？
答案：
当前代码：
function claimReward() external {
    _updateReward(msg.sender);
    StakeInfo storage stakeInfo = userStakes[msg.sender];
    uint256 reward = stakeInfo.rewardDebt;
    require(reward > 0, "no rewards to claim");
    require(rewardToken.balanceOf(address(this)) >= reward, "insufficient reward balance");  // ✓ 已添加
    
    stakeInfo.rewardDebt = 0;
    rewardToken.safeTransfer(msg.sender, reward);
}
问题分析：
场景：
- 合约中有1000 ETH 待领取收益
- 但奖励代币池只剩500 ETH
- 前几个用户领走了500 ETH
- 后面的用户无法领取（revert）

影响：
- 用户体验差
- 可能导致恐慌
- 声誉受损
解决方案：方案1：按比例分配
function claimReward() external {
    _updateReward(msg.sender);
    StakeInfo storage stakeInfo = userStakes[msg.sender];
    uint256 reward = stakeInfo.rewardDebt;
    
    uint256 balance = rewardToken.balanceOf(address(this));
    if (balance < reward) {
        // 按比例分配剩余奖励
        reward = reward * balance / getTotalRewardDebt();
    }
    
    stakeInfo.rewardDebt -= reward;  // 只减去实际发放的部分
    rewardToken.safeTransfer(msg.sender, reward);
}
方案2：紧急充值机制
function emergencyFundReward(uint256 amount) external onlyOwner {
    rewardToken.safeTransferFrom(msg.sender, address(this), amount);
    emit RewardPoolFunded(amount);
}
方案3：暂停领取并通知
function claimReward() external {
    _updateReward(msg.sender);
    StakeInfo storage stakeInfo = userStakes[msg.sender];
    uint256 reward = stakeInfo.rewardDebt;
    
    if (rewardToken.balanceOf(address(this)) < reward) {
        // 不 revert，而是记录欠账
        pendingWithdrawals[msg.sender] += reward;
        stakeInfo.rewardDebt = 0;
        emit RewardPending(msg.sender, reward);
        return;
    }
    
    // 正常发放
    stakeInfo.rewardDebt = 0;
    rewardToken.safeTransfer(msg.sender, reward);
}
扩展题 3：业务逻辑
 问：如何实现带锁仓期的质押？
答案：需求：
用户质押后，必须锁定7天才能解质押
提前解质押需要支付 penalty（罚金）
实现：
struct StakeInfo {
    uint256 amount;
    uint256 rewardDebt;
    uint256 lastUpdate;
    uint256 lockUntil;  // ← 新增：锁仓截止时间
}

uint256 public constant LOCK_PERIOD = 7 days;
uint256 public penaltyRate = 10;  // 10% 罚金

function stake(uint256 _amount) external {
    StakeInfo storage stakeInfo = userStakes[msg.sender];
    _updateReward(stakeInfo);
    
    stakeToken.safeTransferFrom(msg.sender, address(this), _amount);
    stakeInfo.amount += _amount;
    stakeInfo.lastUpdate = block.timestamp;
    stakeInfo.lockUntil = block.timestamp + LOCK_PERIOD;  // ← 设置锁仓期
    
    emit Staked(msg.sender, _amount, block.timestamp);
}

function unstake(uint256 _amount) external {
    StakeInfo storage stakeInfo = userStakes[msg.sender];
    require(stakeInfo.amount >= _amount, "Insufficient balance");
    
    // 检查锁仓期
    if (block.timestamp < stakeInfo.lockUntil) {
        // 提前解质押，收取罚金
        uint256 penalty = _amount * penaltyRate / 100;
        uint256 actualAmount = _amount - penalty;
        
        stakeInfo.amount -= _amount;
        stakeToken.safeTransfer(msg.sender, actualAmount);
        
        // 罚金销毁或转入 treasury
        penaltyToken.burn(penalty);
        
        emit UnstakedWithPenalty(msg.sender, _amount, penalty, block.timestamp);
    } else {
        // 正常解质押
        _updateReward(stakeInfo);
        stakeInfo.amount -= _amount;
        stakeToken.safeTransfer(msg.sender, _amount);
        
        emit Unstaked(msg.sender, _amount, block.timestamp);
    }
}
扩展题 4：升级相关
问：升级合约时需要注意什么？
答案：
注意事项：1. 存储布局兼容性
// V1 版本
contract StakeMiningV1 {
    uint256 public rewardRate;      // slot 0
    mapping(address => uint256) public balances;  // slot 1+
}

// ❌ V2 版本：破坏兼容性
contract StakeMiningV2 {
    uint256 public newVariable;     // slot 0 ← 覆盖了 rewardRate!
    uint256 public rewardRate;      // slot 1 ← 错位了
}

// ✅ V2 版本：保持兼容
contract StakeMiningV2 {
    uint256 public rewardRate;      // slot 0 ← 保持不变
    mapping(address => uint256) public balances;  // slot 1+ ← 保持不变
    uint256 public newVariable;     // slot N ← 新增在后面
}
规则：
✓ 可以新增变量（加在后面）
✗ 不能删除变量
✗ 不能改变变量顺序
✗ 不能改变变量类型
2. 初始化检查
// ❌ 危险：忘记检查是否已初始化
function initializeV2(uint256 _newParam) external {
    newParam = _newParam;  // 可能被重复调用
}

// ✅ 安全：使用 reinitializer
function initializeV2(uint256 _newParam) external reinitializer(2) {
    newParam = _newParam;
}
3. 授权升级的安全性
// ❌ 危险：任何人都能升级
function _authorizeUpgrade(address newImplementation) internal override {
    // 没有检查！
}

// ✅ 安全：只有 owner 能升级
function _authorizeUpgrade(address newImplementation) internal override onlyOwner {
    // 可选：验证新合约
    require(newImplementation != address(0), "Invalid implementation");
    
    // 可选：时间锁
    require(block.timestamp > upgradeDelay, "Upgrade delay not passed");
}
4. 升级前的测试
# 1. 在测试网部署并测试
forge script script/Upgrade.s.sol --fork-url $TESTNET_RPC

# 2. 验证存储布局
cast storage $PROXY_ADDRESS --rpc-url $RPC

# 3. 模拟升级
forge test --match-test testUpgrade -vvv

# 4. 主网升级（多签执行）